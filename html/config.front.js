
var data = {
  "cams" : [
    {"name": "front-surf-report", "duration": 40000},
  ]
};

var contentSequence = ["cams", "cams"]; // these values must match the top level properties of the data object
